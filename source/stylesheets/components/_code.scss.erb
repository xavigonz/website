code,
tt {
  background-color: #f8f8f8;
  border-radius: 2px;
  font-size: .9em;
  font-weight: 700;
  padding: 0.2em 0.4em;
  white-space: pre-wrap;
}

pre {
  code,
  tt {
    font-weight: 400;
    background: none;
    border: none;
    padding: 0;
  }
}

<%= Rouge::Themes::Github.render(scope: '.highlight') %>

.highlight {
  border-radius: 3px;
  padding: 10px;
}
